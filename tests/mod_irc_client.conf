; mod_irc_client.conf

[general]

[bbs]
hostname = 127.0.0.1
port = 6667 ; Since this is a loopback connection, it's not necessary to use TLS for security.
tls = no
sasl = yes
autojoin = #test1,#test2
username = testuser3
password = P@ssw0rD
logfile = no
callbacks = yes ; Must be enabled for mod_irc_relay integration.
