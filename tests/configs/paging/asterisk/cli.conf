[startup_commands]
; Instead of setting 'debug=5' in asterisk.conf, so we only get debug output after Asterisk is fully started.
; We can also filter by module/file, instead of setting global debug.
core set debug 10 app_softmodem = yes
