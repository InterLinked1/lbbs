[modules]
autoload = no

require = app_dial
require = app_softmodem
require = app_tdd
require = bridge_simple
require = codec_ulaw
require = format_pcm
require = format_sln
require = pbx_config
require = res_timing_timerfd

require = func_global
require = func_callerid
require = app_exec ; ExecIf
require = app_originate

require = bridge_softmix

require = app_confbridge
